<p id="notice"><%= notice %></p>

<h1>Listing Product Reviews</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @product_reviews.each do |product_review| %>
      <tr>
        <td><%= link_to 'Edit', edit_product_product_review_path(product_review) %></td>
        <td><%= link_to 'Delete', [product_review.product, product_review], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Product review', new_product_product_review_path %>
